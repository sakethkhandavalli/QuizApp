<%= form_with(model: state, local: true) do |form| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :state_user_id %>
  </div>

  <div class="field">
    <%= form.label :subgenre_id %>
    <%= form.number_field :subgenre_id, id: :state_subgenre_id %>
  </div>

  <div class="field">
    <%= form.label :index %>
    <%= form.number_field :index, id: :state_index %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score, id: :state_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
